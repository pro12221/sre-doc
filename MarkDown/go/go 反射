Go 是静态类型的。每个变量都有一个静态类型，也就是在编译时已知并固定的一种类型：int，float32，* MyType，[] byte 等。如果我们声明
type MyInt int
var i int
var j MyInt
那么我的类型为 int，j 的类型为 MyInt。变量 i 和 j 具有不同的静态类型，尽管它们具有相同的基础类型，但是如果不进行转换就无法将它们彼此赋值。

## Go 接口值的内部表示
核心概念：接口值 = (值, 类型) 对